<?php if(Mage::getBlockSingleton('page/html_header')->getIsHomePage()): ?>
    <script type='text/javascript'>
        // Conversion Name: Homepage 2015
        // INSTRUCTIONS
        // The Conversion Tags should be placed at the top of the <BODY> section of the HTML page.
        // In case you want to ensure that the full page loads as a prerequisite for a conversion
        // being recorded, place the tag at the bottom of the page. Note, however, that this may
        // skew the data in the case of slow-loading pages and in general not recommended.
        //
        // NOTE: It is possible to test if the tags are working correctly before campaign launch
        // as follows:  Browse to http://bs.serving-sys.com/Serving/adServer.bs?cn=at, which is
        // a page that lets you set your local machine to 'testing' mode.  In this mode, when
        // visiting a page that includes an conversion tag, a new window will open, showing you
        // the data sent by the conversion tag to the MediaMind servers.
        //
        // END of instructions (These instruction lines can be deleted from the actual HTML)
        var ebRand = Math.random()+'';
        ebRand = ebRand * 1000000;
        //<![CDATA[
        document.write('<scr'+'ipt src="HTTP://bs.serving-sys.com/Serving/ActivityServer.bs?cn=as&amp;ActivityID=596090&amp;rnd=' + ebRand + '"></scr' + 'ipt>');
        //]]>
    </script>
    <noscript>
        <img width="1" height="1" style="border:0" src="HTTP://bs.serving-sys.com/Serving/ActivityServer.bs?cn=as&amp;ActivityID=596090&amp;ns=1"/>
    </noscript>
<?php elseif(Mage::app()->getFrontController()->getRequest()->getModuleName() == 'catalog' && Mage::registry('current_category') && Mage::registry('current_category')->getName() == 'On Sale Now'): ?>
    <script type='text/javascript'>
        // Conversion Name: On Sale Now 2015
        // INSTRUCTIONS
        // The Conversion Tags should be placed at the top of the <BODY> section of the HTML page.
        // In case you want to ensure that the full page loads as a prerequisite for a conversion
        // being recorded, place the tag at the bottom of the page. Note, however, that this may
        // skew the data in the case of slow-loading pages and in general not recommended.
        //
        // NOTE: It is possible to test if the tags are working correctly before campaign launch
        // as follows:  Browse to http://bs.serving-sys.com/Serving/adServer.bs?cn=at, which is
        // a page that lets you set your local machine to 'testing' mode.  In this mode, when
        // visiting a page that includes an conversion tag, a new window will open, showing you
        // the data sent by the conversion tag to the MediaMind servers.
        //
        // END of instructions (These instruction lines can be deleted from the actual HTML)
        var ebRand = Math.random()+'';
        ebRand = ebRand * 1000000;
        //<![CDATA[
        document.write('<scr'+'ipt src="HTTP://bs.serving-sys.com/Serving/ActivityServer.bs?cn=as&amp;ActivityID=596095&amp;rnd=' + ebRand + '"></scr' + 'ipt>');
        //]]>
    </script>
    <noscript>
        <img width="1" height="1" style="border:0" src="HTTP://bs.serving-sys.com/Serving/ActivityServer.bs?cn=as&amp;ActivityID=596095&amp;ns=1"/>
    </noscript>

    <?php elseif(Mage::app()->getFrontController()->getAction()->getFullActionName() == 'checkout_cart_index'): ?>
    <script type='text/javascript'>
        // Conversion Name: Shopping Cart 2015
        // INSTRUCTIONS
        // The Conversion Tags should be placed at the top of the <BODY> section of the HTML page.
        // In case you want to ensure that the full page loads as a prerequisite for a conversion
        // being recorded, place the tag at the bottom of the page. Note, however, that this may
        // skew the data in the case of slow-loading pages and in general not recommended.
        //
        // NOTE: It is possible to test if the tags are working correctly before campaign launch
        // as follows:  Browse to http://bs.serving-sys.com/Serving/adServer.bs?cn=at, which is
        // a page that lets you set your local machine to 'testing' mode.  In this mode, when
        // visiting a page that includes an conversion tag, a new window will open, showing you
        // the data sent by the conversion tag to the MediaMind servers.
        //
        // END of instructions (These instruction lines can be deleted from the actual HTML)
        var ebRand = Math.random()+'';
        ebRand = ebRand * 1000000;
        //<![CDATA[
        document.write('<scr'+'ipt src="HTTP://bs.serving-sys.com/Serving/ActivityServer.bs?cn=as&amp;ActivityID=596092&amp;rnd=' + ebRand + '"></scr' + 'ipt>');
        //]]>
    </script>
    <noscript>
        <img width="1" height="1" style="border:0" src="HTTP://bs.serving-sys.com/Serving/ActivityServer.bs?cn=as&amp;ActivityID=596092&amp;ns=1"/>
    </noscript>

    <?php elseif(Mage::app()->getFrontController()->getAction()->getFullActionName() == 'onestepcheckout_index_index'): ?>
    <script type='text/javascript'>
        // Conversion Name: One Page Checkout 2015
        // INSTRUCTIONS
        // The Conversion Tags should be placed at the top of the <BODY> section of the HTML page.
        // In case you want to ensure that the full page loads as a prerequisite for a conversion
        // being recorded, place the tag at the bottom of the page. Note, however, that this may
        // skew the data in the case of slow-loading pages and in general not recommended.
        //
        // NOTE: It is possible to test if the tags are working correctly before campaign launch
        // as follows:  Browse to http://bs.serving-sys.com/Serving/adServer.bs?cn=at, which is
        // a page that lets you set your local machine to 'testing' mode.  In this mode, when
        // visiting a page that includes an conversion tag, a new window will open, showing you
        // the data sent by the conversion tag to the MediaMind servers.
        //
        // END of instructions (These instruction lines can be deleted from the actual HTML)
        var ebRand = Math.random()+'';
        ebRand = ebRand * 1000000;
        //<![CDATA[
        document.write('<scr'+'ipt src="HTTPS://bs.serving-sys.com/Serving/ActivityServer.bs?cn=as&amp;ActivityID=596093&amp;rnd=' + ebRand + '"></scr' + 'ipt>');
        //]]>
    </script>
    <noscript>
        <img width="1" height="1" style="border:0" src="HTTPS://bs.serving-sys.com/Serving/ActivityServer.bs?cn=as&amp;ActivityID=596093&amp;ns=1"/>
    </noscript>

    <?php elseif(Mage::app()->getFrontController()->getAction()->getFullActionName() == 'locator_index_index'): ?>
    <script type='text/javascript'>
        // Conversion Name: Store Locator 2015
        // INSTRUCTIONS
        // The Conversion Tags should be placed at the top of the <BODY> section of the HTML page.
        // In case you want to ensure that the full page loads as a prerequisite for a conversion
        // being recorded, place the tag at the bottom of the page. Note, however, that this may
        // skew the data in the case of slow-loading pages and in general not recommended.
        //
        // NOTE: It is possible to test if the tags are working correctly before campaign launch
        // as follows:  Browse to http://bs.serving-sys.com/Serving/adServer.bs?cn=at, which is
        // a page that lets you set your local machine to 'testing' mode.  In this mode, when
        // visiting a page that includes an conversion tag, a new window will open, showing you
        // the data sent by the conversion tag to the MediaMind servers.
        //
        // END of instructions (These instruction lines can be deleted from the actual HTML)
        var ebRand = Math.random()+'';
        ebRand = ebRand * 1000000;
        //<![CDATA[
        document.write('<scr'+'ipt src="HTTP://bs.serving-sys.com/Serving/ActivityServer.bs?cn=as&amp;ActivityID=596091&amp;rnd=' + ebRand + '"></scr' + 'ipt>');
        //]]>
    </script>
    <noscript>
        <img width="1" height="1" style="border:0" src="HTTP://bs.serving-sys.com/Serving/ActivityServer.bs?cn=as&amp;ActivityID=596091&amp;ns=1"/>
    </noscript>

    <?php elseif(Mage::app()->getFrontController()->getRequest()->getModuleName() == 'checkout' && Mage::app()->getRequest()->getActionName() == 'success' && Mage::getSingleton('checkout/session')->getLastOrderId()): ?>
    <?php
        $orderId = "";
        $productIds = "";
        $productInfo = "";
        $quantity ="";
        $total="";
        $info = array();
        $ids = array();

        $order= Mage::getModel('sales/order')->load(Mage::getSingleton('checkout/session')->getLastOrderId());
        $orderId = $order->getId();
        $items = $order->getAllVisibleItems();
        $quantity = 0;
        foreach($items as $item)
        {
            $quantity += $item->getQtyOrdered();
            $product = Mage::getModel('catalog/product')->load($item->getProductId());
            $ids[]= $item->getProductId();
            $info[] = $item->getName();
        }
        $productIds = implode("|",$ids);
        $productInfo = implode("|",$info);
        $total = round($order->getGrandTotal(), 2);
    ?>
    <script type='text/javascript'>
        // Conversion Name: Order & Payment Confirmation 2015
        // INSTRUCTIONS to site webmaster regarding parameters
        // to be dynamically entered by the site web/content server:
        //
        // In the code below, you'll find a [Revenue] parameter and/or a [Quantity] parameter and/or an
        // [OrderID],[ProductID][ProductInfo] parameter.
        // The [Revenue] parameter is to be dynamically set to reflect the value currently stored within the user's shopping cart.
        // The parameter should be a decimal number, for example:  var ebRev = '229.88';
        // The [Quantity] parameter is to be dynamically set to reflect the number of items currently stored within the user's shopping cart.
        // The parameter should be an integer, for example:  var ebQuantity = '3';
        // The [OrderID],[ProductID][ProductInfo] parameters are to be dynamically set to reflect
        // the order ID for this transaction. You should verify with your MediaMind
        // Account Manager that your account has the extended data collected in order for this parameter to be recorded.
        // This parameter should be a string with the max length of 36 characters
        // The parameter may be a string, for example:  var ebOrderID = '1234-5678-90';
        // The Product ID parameter should be a string with the max length of 36 characters.
        // For example: var ebProductID = �ax-34579-989';
        // The Product Info parameter should be a string with the max length of 100 characters.
        // For example: var ebProductInfo = �Colour blue, size XL..';
        //
        // The Conversion Tags should be placed at the top of the <BODY> section of the HTML page.
        //
        // NOTE: It is possible to test if the tags are working correctly before campaign launch
        // as follows:  Browse to http://bs.serving-sys.com/Serving/adServer.bs?cn=at, which is
        // a page that lets you set your local machine to 'testing' mode.  In this mode, when
        // visiting a page that includes an conversion tag, a new window will open, showing you
        // the data sent by the conversion tag to the MediaMind servers.
        //
        // END of instructions (These instruction lines can be deleted from the actual HTML)
        var ebRev = '<?php echo round($total, 2);?>';
        var ebOrderID = '<?php echo $orderId;?>';
        var ebProductID = '<?php echo $productIds;?>';
        var ebProductInfo = '<?php echo $productInfo;?>';
        var ebRand = Math.random()+'';
        ebRand = ebRand * 1000000;
        //<![CDATA[
        document.write('<scr'+'ipt src="HTTPS://bs.serving-sys.com/Serving/ActivityServer.bs?cn=as&amp;ActivityID=596094&amp;rnd=' + ebRand + '&amp;Value='+ebRev+'&amp;OrderID='+ebOrderID+'&amp;ProductID='+ebProductID+'&amp;ProductInfo='+ebProductInfo+'"></scr' + 'ipt>');
        //]]>
    </script>
<noscript>
    <img width="1" height="1" style="border:0" src="HTTPS://bs.serving-sys.com/Serving/ActivityServer.bs?cn=as&amp;ActivityID=596094&amp;Value=[Revenue]&amp;OrderID=[OrderID]&amp;ProductID=[ProductID]&amp;ProductInfo=[ProductInfo]&amp;ns=1"/>
</noscript>
<?php endif;?>